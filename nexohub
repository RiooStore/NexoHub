-- === RIOOHUB V27 (CORE INJECTION - MULTI UNIT PERFECT FIXED) ===
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

-- Anti Double UI
if PlayerGui:FindFirstChild("RiooHub_ZoneEngine") then 
    PlayerGui.RiooHub_ZoneEngine:Destroy() 
end

-- Core Config & States
local FILE_NAME = "rioo_precise_macro.txt"
local isRecording = false
local StartTime = 0
local MacroData = {}
local currentSpeedSetting = 3
local speedToggleActive = false
local autoReplayEnabled = false
local autoSkipEnabled = false
local autoVoteReadyEnabled = false
local targetDifficulty = "Easy"

-- SAFE REMOTE MANAGER
local function getSafeRemote(remoteName)
    local eventsFolder = ReplicatedStorage:FindFirstChild("Events") or ReplicatedStorage:FindFirstChild("Remotes")
    if eventsFolder then
        return eventsFolder:FindFirstChild(remoteName)
    end
    return nil
end

-- REAL-TIME REMOTE INJECTION (Sistem Sadap ID Unit)
local placeRemote = getSafeRemote("PlaceTower")
if placeRemote and placeRemote:IsA("RemoteFunction") then
    local oldInvoke = placeRemote.InvokeServer
    placeRemote.InvokeServer = function(self, ...)
        local args = {...}
        
        -- Jika macro sedang merekam, sadap data langsung dari tangan player
        if isRecording and args[1] and args[2] then
            local currentDelay = os.clock() - StartTime
            local coords = args[1] -- Vector3 asli game
            local unitUID = args[2] -- ID Unit asli yang beneran ditaruh
            
            table.insert(MacroData, {
                Type = "Spawn",
                Time = currentDelay,
                X = coords.X,
                Y = coords.Y,
                Z = coords.Z,
                SavedUID = unitUID -- Disimpan murni sesuai unit yg dipasang!
            })
            
            -- Trigger refresh visual log biar keliatan ID nya berubah
            local shortID = string.sub(tostring(unitUID), 1, 8) .. "..."
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "RiooHub Recorder",
                Text = "Captured Unit ID: " .. shortID,
                Duration = 1.5
            })
        end
        return oldInvoke(self, unpack(args))
    end
end

-- MULTI-REMOTE FALLBACK FOR REPLAY
local function eksekusiPlayAgain()
    local replayRemote = getSafeRemote("PlayAgain") or getSafeRemote("Replay") or getSafeRemote("Retry")
    if replayRemote then
        replayRemote:FireServer()
        return true
    end
    return false
end

local function eksekusiVoteDanReady()
    local voteRemote = getSafeRemote("VoteDifficulty")
    if voteRemote then voteRemote:FireServer(targetDifficulty) end
    task.wait(0.3)
    local readyRemote = getSafeRemote("Ready")
    if readyRemote then readyRemote:FireServer() end
end

-- =========================================================================
-- INTERFACE BASE LAYOUT
-- =========================================================================
local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.Name = "RiooHub_ZoneEngine"
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 580, 0, 420)
Main.Position = UDim2.new(0.5, -290, 0.5, -210)
Main.BackgroundColor3 = Color3.fromRGB(15, 11, 25)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0, 10)

local MainStroke = Instance.new("UIStroke", Main)
MainStroke.Color = Color3.fromRGB(230, 100, 230)
MainStroke.Thickness = 1
MainStroke.Transparency = 0.3

-- MINIMIZE SYSTEM
local MinBtn = Instance.new("TextButton", ScreenGui)
MinBtn.Size = UDim2.new(0, 45, 0, 45)
MinBtn.Position = UDim2.new(0.02, 0, 0.4, 0)
MinBtn.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
MinBtn.Text = "Z"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 20
MinBtn.Visible = false
MinBtn.Active = true
MinBtn.Draggable = true

Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(1, 0)
local MinStroke = Instance.new("UIStroke", MinBtn)
MinStroke.Color = Color3.fromRGB(230, 100, 230)
MinStroke.Thickness = 1.5

local CloseToggle = Instance.new("TextButton", Main)
CloseToggle.Size = UDim2.new(0, 30, 0, 30)
CloseToggle.Position = UDim2.new(1, -40, 0, 10)
CloseToggle.BackgroundTransparency = 1
CloseToggle.Text = "—"
CloseToggle.TextColor3 = Color3.fromRGB(230, 100, 230)
CloseToggle.Font = Enum.Font.GothamBold
CloseToggle.TextSize = 16

CloseToggle.MouseButton1Click:Connect(function()
    Main:TweenSize(UDim2.new(0, 0, 0, 0), "Out", "Quad", 0.15, true, function()
        Main.Visible = false
        MinBtn.Visible = true
    end)
end)

MinBtn.MouseButton1Click:Connect(function()
    MinBtn.Visible = false
    Main.Visible = true
    Main:TweenSize(UDim2.new(0, 580, 0, 420), "Out", "Quad", 0.15, true)
end)

-- SIDEBAR
local Sidebar = Instance.new("Frame", Main)
Sidebar.Size = UDim2.new(0, 50, 1, 0)
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
Sidebar.BorderSizePixel = 0
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 10)

local SidebarLine = Instance.new("Frame", Main)
SidebarLine.Size = UDim2.new(0, 1, 1, 0)
SidebarLine.Position = UDim2.new(0, 50, 0, 0)
SidebarLine.BackgroundColor3 = Color3.fromRGB(230, 100, 230)
SidebarLine.BorderSizePixel = 0
SidebarLine.BackgroundTransparency = 0.7

-- TOPBAR
local Topbar = Instance.new("Frame", Main)
Topbar.Size = UDim2.new(1, -50, 0, 50)
Topbar.Position = UDim2.new(0, 50, 0, 0)
Topbar.BackgroundTransparency = 1

local Title = Instance.new("TextLabel", Topbar)
Title.Size = UDim2.new(0, 200, 0, 25)
Title.Position = UDim2.new(0, 15, 0, 8)
Title.BackgroundTransparency = 1
Title.Text = "Main Engine"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

local Subtitle = Instance.new("TextLabel", Topbar)
Subtitle.Size = UDim2.new(0, 350, 0, 15)
Subtitle.Position = UDim2.new(0, 15, 0, 26)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Full AFK Automation (RiooHub V27)"
Subtitle.TextColor3 = Color3.fromRGB(130, 115, 150)
Subtitle.Font = Enum.Font.SourceSans
Subtitle.TextSize = 12
Subtitle.TextXAlignment = Enum.TextXAlignment.Left

local Container = Instance.new("Frame", Main)
Container.Size = UDim2.new(1, -70, 1, -70)
Container.Position = UDim2.new(0, 60, 0, 60)
Container.BackgroundTransparency = 1

local function createZoneSection(name, iconText, size, pos)
    local box = Instance.new("Frame", Container)
    box.Size = size
    box.Position = pos
    box.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
    box.BorderSizePixel = 0
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)
    
    local stk = Instance.new("UIStroke", box)
    stk.Color = Color3.fromRGB(230, 100, 230)
    stk.Thickness = 0.8
    stk.Transparency = 0.6
    
    local titleLbl = Instance.new("TextLabel", box)
    titleLbl.Size = UDim2.new(1, -10, 0, 25)
    titleLbl.Position = UDim2.new(0, 10, 0, 4)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = iconText .. "  " .. name
    titleLbl.TextColor3 = Color3.fromRGB(240, 220, 255)
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextSize = 11
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    
    return box
end

local LeftBox = createZoneSection("Macro Automation", "📈", UDim2.new(0, 245, 0, 195), UDim2.new(0, 0, 0, 0))
local RightBox = createZoneSection("Match Settings", "⚙️", UDim2.new(0, 245, 0, 195), UDim2.new(0, 260, 0, 0))
local StatusBox = createZoneSection("Console Log Activity", "📋", UDim2.new(1, -5, 0, 140), UDim2.new(0, 0, 0, 210))

local LogScroll = Instance.new("ScrollingFrame", StatusBox)
LogScroll.Size = UDim2.new(1, -20, 1, -35)
LogScroll.Position = UDim2.new(0, 10, 0, 30)
LogScroll.BackgroundTransparency = 1
LogScroll.BorderSizePixel = 0
LogScroll.CanvasSize = UDim2.new(0, 0, 4, 0)
LogScroll.ScrollBarThickness = 3
LogScroll.ScrollBarImageColor3 = Color3.fromRGB(230, 100, 230)

local StatusLog = Instance.new("TextLabel", LogScroll)
StatusLog.Size = UDim2.new(1, 0, 1, 0)
StatusLog.BackgroundTransparency = 1
StatusLog.Text = "[SYSTEM]: V27 Active. Core Injection Remote Sniffer Armed."
StatusLog.TextColor3 = Color3.fromRGB(170, 150, 190)
StatusLog.Font = Enum.Font.SourceSans
StatusLog.TextSize = 12
StatusLog.TextXAlignment = Enum.TextXAlignment.Left
StatusLog.TextYAlignment = Enum.TextYAlignment.Top
StatusLog.TextWrapped = true

local function appendZoneLog(actionType, text)
    local timestamp = os.date("%X")
    StatusLog.Text = string.format("[%s] [%s] %s\n", timestamp, string.upper(actionType), text) .. StatusLog.Text
end

local function createZoneButton(text, pos, size, parent, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = size
    btn.Position = pos
    btn.BackgroundColor3 = Color3.fromRGB(26, 18, 42)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(240, 240, 240)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    btn.BorderSizePixel = 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    
    local bstk = Instance.new("UIStroke", btn)
    bstk.Color = Color3.fromRGB(230, 100, 230)
    bstk.Thickness = 0.8
    bstk.Transparency = 0.5
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local function createZoneToggle(text, pos, parent, callback)
    local row = Instance.new("Frame", parent)
    row.Size = UDim2.new(1, -20, 0, 26)
    row.Position = pos
    row.BackgroundTransparency = 1
    
    local txt = Instance.new("TextLabel", row)
    txt.Size = UDim2.new(0, 160, 1, 0)
    txt.BackgroundTransparency = 1
    txt.Text = text
    txt.TextColor3 = Color3.fromRGB(190, 180, 200)
    txt.Font = Enum.Font.SourceSans
    txt.TextSize = 13
    txt.TextXAlignment = Enum.TextXAlignment.Left
    
    local switch = Instance.new("TextButton", row)
    switch.Size = UDim2.new(0, 32, 0, 16)
    switch.Position = UDim2.new(1, -32, 0, 5)
    switch.BackgroundColor3 = Color3.fromRGB(45, 33, 62)
    switch.Text = ""
    Instance.new("UICorner", switch).CornerRadius = UDim.new(1, 0)
    
    local circle = Instance.new("Frame", switch)
    circle.Size = UDim2.new(0, 10, 0, 10)
    circle.Position = UDim2.new(0, 3, 0, 3)
    circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", circle).CornerRadius = UDim.new(1, 0)
    
    local active = false
    switch.MouseButton1Click:Connect(function()
        active = not active
        local targetPos = active and UDim2.new(1, -13, 0, 3) or UDim2.new(0, 3, 0, 3)
        local targetColor = active and Color3.fromRGB(230, 100, 230) or Color3.fromRGB(45, 33, 62)
        
        TweenService:Create(circle, TweenInfo.new(0.12), {Position = targetPos}):Play()
        TweenService:Create(switch, TweenInfo.new(0.12), {BackgroundColor3 = targetColor}):Play()
        
        callback(active)
    end)
end

-- LOOPS MANAGEMENT
local function paksaSpeedGame(targetSpeed)
    local speedRemote = getSafeRemote("RequestGameSpeed")
    if speedRemote then
        task.spawn(function()
            speedRemote:FireServer(targetSpeed)
            task.wait(0.15)
            speedRemote:FireServer(targetSpeed)
        end)
    end
end

task.spawn(function()
    while task.wait(1) do
        if autoSkipEnabled then
            local skipRemote = getSafeRemote("VoteSkip")
            if skipRemote then skipRemote:FireServer(true) end
        end
    end
end)

task.spawn(function()
    while task.wait(1.5) do
        if autoVoteReadyEnabled then eksekusiVoteDanReady() end
    end
end)

task.spawn(function()
    while task.wait(3) do
        if autoReplayEnabled then
            local berhasil = eksekusiPlayAgain()
            if berhasil then appendZoneLog("Replay", "Spamming PlayAgain remote...") end
        end
    end
end)

-- SMART INJECTED RECORDING BUTTON
local RecordBtn
RecordBtn = createZoneButton("START MACRO RECORD", UDim2.new(0, 10, 0, 35), UDim2.new(1, -20, 0, 32), LeftBox, function()
    if not isRecording then
        MacroData = {}
        StartTime = os.clock()
        isRecording = true
        
        RecordBtn.Text = "🛑 STOP RECORDING"
        RecordBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
        appendZoneLog("System", "Recording via Remote Sniffer. Place your units normally!")
    else
        isRecording = false
        RecordBtn.Text = "START MACRO RECORD"
        RecordBtn.TextColor3 = Color3.fromRGB(240, 240, 240)
        appendZoneLog("System", string.format("Saved! Actions: %d", #MacroData))
        
        if #MacroData > 0 and writefile then
            writefile(FILE_NAME, HttpService:JSONEncode(MacroData))
        end
    end
end)

local function muatDataMakro()
    if readfile and isfile and isfile(FILE_NAME) then
        MacroData = HttpService:JSONDecode(readfile(FILE_NAME))
        appendZoneLog("Load", string.format("Loaded macro. %d multi-units armed.", #MacroData))
        return true
    end
    return false
end
createZoneButton("LOAD DATA FROM TEXT", UDim2.new(0, 10, 0, 75), UDim2.new(1, -20, 0, 32), LeftBox, muatDataMakro)

local function jalankanPlayback()
    if #MacroData == 0 then return end
    appendZoneLog("Play", "Running macro playback...")
    task.spawn(function()
        if speedToggleActive then paksaSpeedGame(currentSpeedSetting) end
        table.sort(MacroData, function(a, b) return a.Time < b.Time end)
        local playbackStart = os.clock()
        
        for index = 1, #MacroData do
            local step = MacroData[index]
            local adjustedTime = speedToggleActive and (step.Time / 1.5) or step.Time
            while (os.clock() - playbackStart) < adjustedTime do task.wait(0.01) end
            
            local placeRemote = getSafeRemote("PlaceTower")
            if step.Type == "Spawn" and placeRemote and step.SavedUID then
                local finalArgs = {
                    [1] = Vector3.new(step.X, step.Y, step.Z),
                    [2] = step.SavedUID, -- Menaruh unit asli sesuai rekaman per-detik
                    [3] = 0
                }
                task.spawn(function() placeRemote:InvokeServer(unpack(finalArgs)) end)
                appendZoneLog("Spawn", string.format("Sequence %d deployed successfully.", index))
            end
        end
        appendZoneLog("System", "All sequences deployed.")
    end)
end
createZoneButton("RUN AUTOMATION", UDim2.new(0, 10, 0, 115), UDim2.new(1, -20, 0, 32), LeftBox, jalankanPlayback)

-- MATCH SETTINGS
createZoneToggle("Force Game Speed x2", UDim2.new(0, 10, 0, 32), RightBox, function(state)
    speedToggleActive = state
    if state then currentSpeedSetting = 2; paksaSpeedGame(2) else paksaSpeedGame(1) end
end)

createZoneToggle("Force Game Speed x3 (Pass)", UDim2.new(0, 10, 0, 60), RightBox, function(state)
    speedToggleActive = state
    if state then currentSpeedSetting = 3; paksaSpeedGame(3) else paksaSpeedGame(1) end
end)

createZoneToggle("Auto Skip Waves", UDim2.new(0, 10, 0, 88), RightBox, function(state)
    autoSkipEnabled = state
end)

createZoneToggle("Auto Vote Easy + Ready", UDim2.new(0, 10, 0, 116), RightBox, function(state)
    autoVoteReadyEnabled = state
    if state then eksekusiVoteDanReady() end
end)

createZoneToggle("Auto Play Again (Loop)", UDim2.new(0, 10, 0, 144), RightBox, function(state)
    autoReplayEnabled = state
    if state then eksekusiPlayAgain() end
    if writefile then writefile("rioo_autoreplay_state.txt", tostring(autoReplayEnabled)) end
end)

-- AUTO RUN HOOK
task.spawn(function()
    task.wait(3)
    local suksesLoad = muatDataMakro()
    if suksesLoad and isfile and isfile("rioo_autoreplay_state.txt") then
        local state = readfile("rioo_autoreplay_state.txt")
        if state == "true" then
            autoReplayEnabled = true
            task.wait(1)
            jalankanPlayback()
        end
    end
end)
